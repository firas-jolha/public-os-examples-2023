#include <stdio.h>

int main() {
    int hexNumber = 0x1A; // Example hexadecimal number

    // Write the hexadecimal number to stdout
    printf("Hexadecimal number: 0x%x\n", hexNumber);

    return 0;
}
